{
  "name": "Python 3",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.11-bookworm",
  "customizations": {
    "codespaces": {
      "openFiles": [
        "README.md",
        "main.py"
      ]
    },
    "vscode": {
      "settings": {},
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance"
      ]
    }
  },
  "updateContentCommand": "if [ -f packages.txt ]; then sudo apt-get update && sudo apt-get install -y $(cat packages.txt); fi && python3 -m pip install --upgrade pip setuptools wheel && if [ -f requirements.txt ]; then pip3 install -r requirements.txt; fi && echo '✅ setup finished'",
  "postStartCommand": "streamlit run main.py --server.port 8501 --server.enableCORS false --server.enableXsrfProtection false",
  "portsAttributes": {
    "8501": {
      "label": "Application",
      "onAutoForward": "openPreview"
    }
  },
  "forwardPorts": [
    8501
  ]
}
